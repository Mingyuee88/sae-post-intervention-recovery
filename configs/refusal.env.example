# Copy this file, edit paths, then source it before running scripts/reproduce_refusal.sh.

export ADV_BENCH_STRICT_VALID_TARGET_PAIRS_JSON=/path/to/local/advbench_target_pairs.json
export OUTPUT_DIR=runs/refusal/advbench_jacobian_v4
